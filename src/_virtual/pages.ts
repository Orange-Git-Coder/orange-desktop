/**
 * Stub for vite-plugin-pages generated routes.
 * At build time, Vite replaces this with the actual generated routes
 * from `vite-plugin-pages`. This stub exists solely so Deno's LSP
 * can resolve the `~pages` import.
 */
import type { RouteRecordRaw } from "vue-router"

const routes: RouteRecordRaw[] = []

export default routes